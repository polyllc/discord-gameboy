
export enum ModeEnum {
    delete = 1,
    continuous = 2,
    edit = 3
}

// cache paths
export const IMG_PATH = 'img/'
export const SAVES_PATH = 'saves/'
export const GAMES_PATH = 'games/'

// canvas
export const defaultCanvasWidth = 144;
export const defaultCanvasHeight = 160;
export const defaultGIFLength = 3000; // gif length in ms

// TODO make prefix dynamic (changable per guild)
export const PREFIX = 'gb!'
